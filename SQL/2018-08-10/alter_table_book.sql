ALTER TABLE `tplay_book` CHANGE COLUMN `type` `type` varchar(60) NOT NULL COMMENT '子分类', ADD COLUMN `ptype` varchar(30) NOT NULL COMMENT '父分类' AFTER `update_time`;

ALTER TABLE `tplay_book` CHANGE COLUMN `ptype` `ptype` varchar(30) NOT NULL COMMENT '父分类' AFTER `avatar`, CHANGE COLUMN `type` `type` varchar(60) NOT NULL COMMENT '子分类' AFTER `ptype`, CHANGE COLUMN `status` `status` tinyint(1) NOT NULL COMMENT '状态 1完结 2连载' AFTER `type`, CHANGE COLUMN `is_free` `is_free` tinyint(1) NOT NULL DEFAULT 1 COMMENT '是否免费 1免费 2收费' AFTER `status`, ADD COLUMN `td_free` tinyint(1) NOT NULL DEFAULT 2 COMMENT '今日免费  1免费 2未免费' AFTER `is_free`, CHANGE COLUMN `is_chapter` `is_chapter` tinyint(1) NOT NULL COMMENT '是否按章收费 1是 2不是' AFTER `td_free`, CHANGE COLUMN `create_time` `create_time` int(11) NOT NULL COMMENT '添加时间' AFTER `update_time`;